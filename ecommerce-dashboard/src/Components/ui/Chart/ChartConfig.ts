export const chartColors = {
  revenue: 'hsl(var(--primary))', // Main brand color
  orders: 'hsl(var(--secondary))', // Secondary color
  stock: 'hsl(var(--destructive))', // For out of stock/low stock
  profit: '#10b981', // Emerald 500 for profit
  expenses: '#ef4444', // Red 500 for expenses
  customers: '#3b82f6', // Blue 500
  grid: 'hsl(var(--border))',
  text: 'hsl(var(--muted-foreground))',
  tooltipBg: 'hsl(var(--background))',
  tooltipBorder: 'hsl(var(--border))',
  tooltipText: 'hsl(var(--foreground))',
};

export const defaultChartMargins = {
  top: 10,
  right: 10,
  left: -20,
  bottom: 0,
};
